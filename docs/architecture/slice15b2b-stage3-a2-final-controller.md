# Stage-III A2 final one-shot controller (dormant source contract)

Status: source-only. `scripts/memory_broker_stage3_final.py` has no CLI, workflow, installation, dispatch, or DEV execution authorization. The protected-main base is `d3d9ba3d57ad3dc69069be1ed3347b81f991cd3a`. Its exact candidate remains `4a04f2d09a2b32aecedfe777090fd2e1a27ec909`. This change does not alter that release, the verifier, broker IPC, A2 seam, relay's general behavior, or synthetic credential custody.

The future, separately authorized caller must keep one root `FinalController` alive in one process. The existing systemd guard binds its pidfd to that process; killing or losing the controller while experimental bindings are active makes broker and socket inert independently of Python exception handling. There is no resume, replay, second authorization, or variable target input. A root-owned, fsynced, exclusive attempt tombstone is written before JIT V2 issuance so even a pre-transition process death is a terminal attempt. The existing transition's root-only phase journal independently claims the one experimental fork before switching any binding.

The fixed sequence is: issue one closed V2 authorization; activate the already-reviewed reversible transition; prepare one JIT synthetic operation using the existing relay, terminalizing that V2 marker; freeze its exact assertion frame in the reviewed adapter; write the exact `Stage3FaultArmV1` bytes under root:broker `0750/0640` custody; and open a pidfd for the verified candidate service process before submitting that frame. The controller never sends `SIGSTOP`: only the candidate's exact A2 fault seam can self-stop after proof consumption and before claim.

Barrier acceptance requires all of these together: the original pidfd is still live; systemd still reports the same active invocation, PID, and zero restarts; `/proc` shows the same broker UID/GID, exact candidate command, boot ID, and start ticks with state `T`; journald's trusted PID, boot, unit, and invocation fields bind exactly one canonical `STAGE3_A2_BARRIER_V1` line to that process and the prepared challenge/request/action within the arm window; and the experimental fork shows `CONSUMED` with no claim or synthetic evidence. The stopped owner WAL is hashed and bounded. Only its hash and the non-secret barrier line are preserved in root-only evidence, not the assertion.

Before a kill, the controller verifies and removes the exact arm and fsyncs its directory. It rechecks the stopped process and barrier, writes an exclusive kill-attempt receipt, and sends `SIGKILL` only through the already-open pidfd. A pidfd-exit observation is recorded; an ambiguous signal or exit cannot be retried. The existing service's `Restart=on-failure` is observed, not replaced by generic service control. Exactly one new candidate invocation and one restart, candidate health, intact guard, absent arm, and consumed/no-claim state are required. The broker-UID helper then calls the existing pinned `recover(challenge_id)` once against the experimental fork. The relay-UID sender replays the **same in-memory frame bytes** once; EOF, unchanged consumed/no-claim/no-evidence state, healthy broker, and no additional A2 barrier justify the closed `REJECTED_BEFORE_CLAIM` report outcome. No private verifier code is claimed from the socket.

`Stage3A2CompletedEvidenceV1` is assembled only after those independent observations and passed to the already-merged `seal_evidence()` contract. The existing `restore()` then closes the gate, retains the experimental fork and evidence, reinstates accepted bytes/config/selector while inert, and validates the separate restored profile. A root-only `Stage3A2FinalReportV1` contains only fixed identities, digests, and the restored invocation; neither report stores assertion bytes. Any ambiguity while a transition vault exists invokes the existing fail-inert containment; journal unreadability still causes an attempt to close the OS gate and stop both units. There is no automatic rollback from a partial phase.

Focused tests use synthetic process/journal/systemd observations and mocks to prove exact binding, stop-before-kill ordering, durable kill intent, one restart, no proof in evidence, restoration ordering, and one-shot denial. A Linux-only test sends a pidfd signal to a disposable self-stopped synthetic child; it never addresses a LILITH process. Existing real-systemd guard/gate tests remain the OS-level evidence for liveness containment. These new tests do not exercise real LILITH authority, DEV, or the A2 experiment. Operational acceptance still requires a separately authorized isolated end-to-end controller validation and explicit DEV execution authority.
