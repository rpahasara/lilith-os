# Slice 15B2b-B1b-2b Stage-II runtime control: API baseline repair

## Research record

**STAGE-II CONTROL MODEL DEFECT — VOLATILE DEV API PROCESS IDENTITY WAS INCORRECTLY ENCODED AS A DURABLE SOURCE-CONTROL INVARIANT.** The original trusted Stage-II source required Core API PID `80100` and its September 23 start time. A separately owner-authorized bootstrap restart legitimately changed that process identity while the service remained healthy and its trusted configuration and custody were unchanged. Read-only Stage-II preflight therefore stopped before authorization or activation. This was a control-model defect, not an API deployment defect.

The correction pins durable DEV API identity and custody in trusted source, then captures volatile process identity for each future authorization. It does not pin replacement PID `88740` or its start time in source. The old and new identities are test fixtures only.

## Durable and observed boundaries

The pinned DEV host check still requires exact GCE project, zone, instance ID/name, local FQDN, and machine ID, and explicitly rejects PROD. Stage-I release, identities, configuration, database custody, and inert broker/socket checks remain unchanged. API service name, loaded/active/running state, `lilith` user/group, exact working directory, fragment path, absence of drop-ins, executable and command line, app SHA-256, API unit SHA-256, canonical-runtime and legacy DEV DB SHA-256, `NRestarts=0`, and healthy `/health` result are durable acceptance conditions. PID must be positive and start time present, but neither value is source-pinned. The exact unit hash also anchors the on-disk service command and configuration.

`ApiRuntimeBaselineV1` is a closed record with `schemaVersion=1`, service name, `MainPID`, `ExecMainStartTimestamp`, `NRestarts`, active/substate, service user/group, working directory, observed `ExecStart`, app and unit SHA-256, health result, custody hashes, and UTC capture timestamp. Its digest is SHA-256 of UTF-8 JSON with sorted keys and compact separators, matching the existing marker serialization convention. `ExecStart` includes the process start and PID as observed by systemd, so session drift is also visible there.

The Stage-II authorization marker is now a closed schema version 2 record. It contains the full trusted API baseline and `apiBaselineDigest`; schema version 1 is rejected. Before the single-use marker is consumed or any activation mutation, the control reruns the complete inert Stage-I/API preflight and immediately re-observes the API against the marker-bound baseline. It does not silently recapture or replace authorization when PID, start time, restart count, health, command, binary, unit, or custody changes. After the Stage-II experiment, it compares the complete API observation again using the authorized capture timestamp. Any drift is an API non-interference failure; existing failure containment stops broker and socket and preserves evidence.

This control-only repair grants no activation. The prior owner activation decision is not carried forward. After a protected merge and read-only preflight, a fresh owner decision is required before Stage-II marker creation or runtime mutation. No Stage-III, PROD, IAM, real-credential, live-authority, or real-memory capability is introduced.

## Retry #2 control repair after failed-inert run 35924789001

The V1 API baseline above remains the historical meaning of the consumed attempt-#1 marker; it is not reinterpreted. Its raw `ExecStart` string contained systemd runtime annotations (`pid=` and `start_time=`). A daemon-reload on the failed run changed those display annotations to `pid=0` and `start_time=[n/a]` while the actual API PID 88740, process start, restart count, app/unit/custody hashes, and health did not change. Comparing the entire string incorrectly reported an API restart.

`ApiRuntimeBaselineV2` (`schemaVersion=2`) separates configured identity from incarnation. Configured identity includes the exact service, host/machine, user/group, working directory, normalized `ExecStart` executable and arguments, app/unit SHA-256, and custody hashes. `ExecStart`'s runtime annotations are parsed only as display context and excluded from its digest. Incarnation includes `MainPID`, systemd's actual `ExecMainStartTimestamp`, `NRestarts`, active/substate, Linux boot ID plus `/proc/<pid>/stat` start ticks, the actual executable and complete command line, and health. A future marker captures the complete V2 record and digest. Immediate pre-consumption and post-experiment observations compare this complete security-relevant record with a fixed capture timestamp. PID, kernel start ticks, restart count, configured command, executable/cmdline, host, health, app, unit, or custody drift fails closed; a bookkeeping-only daemon-reload does not.

The new closed Stage-II marker is schema version 3. It requires V2 API identity and binds a `Stage2RetryBaselineV1` digest. Schema-2/V1 markers cannot be used under V2 semantics. The original used marker remains terminal and byte-for-byte pinned at `b1b2b-stage2-authorization.used.json`; retry #2 would consume its distinct marker into `b1b2b-stage2-retry2-authorization.used.json`. A new authorization ID must differ from `4006ead71ca04c219b3b41e6818010f2`. This control PR creates no marker and performs no retry.

`Stage2RetryBaselineV1` admits only exact `POST_STAGE_II_FAILED_INERT_V1` history. Its pinned input is historical digest `8068e066743d70252a6fd0f0e6fdbab12c1be2c9ac00e152c4746ee810fcfe83`, two exact DB file hashes and sidecar hashes, schema fingerprints, the old used marker hash/authorization, 12 challenge IDs/states, 12 request IDs/digests, one claim, and the existing `se.68b647fc14aeea4fb32bdd7d0fada54d8f4af7c272ec6784f5d22c4c8980e7b5` evidence. Complete historical row hashes include opaque payloads and timestamps without disclosing them. Static rows and SQL schemas are also fingerprinted. The baseline records the inactive broker/socket and empty root:ipc `0710` runtime directory; it is evidence, not activation authority. Trusted preflight and an immediate pre-consumption recheck compare its digest before the first mutation.

A future retry tests exactly 12 *new* challenge/request pairs: one consumed successful confirmation, ten cancelled, and one expired. Its one new claim and `se.*` evidence must link to that success and agree on challenge and request/action digests. Replay, cancel, expiry, invalid assertions, and binding mismatches add no evidence. End state is historical 12/12/1/1 plus new 12/12/1/1, never a zero-history assumption. Every historical row hash, static row, SQL schema, and old used marker must remain exact; any unexplained new row or old-row edit/relink fails. Reports label attempt-#1 evidence separately from retry evidence. Failure containment preserves both attempts' terminal history; no deletion, rollback, or reuse of the old authorization occurs.

The prior IPC sequence ran relay HEALTH before the first negative peer probes. The first repair created the runtime directory, started only the socket, verified the service was still inactive, probed `lilith` and unrelated `nobody` for filesystem/socket denial, then verified inactivity again before relay HEALTH. That still left a gap between the two negative probes. The execution-gate repair independently verifies broker inactivity immediately after *each* probe before allowing the next one. After relay HEALTH it observes actual `SO_PEERCRED`, probes the broker UID as a client, and retains forged-message identity rejection. Directory/socket denial is valid Layer-1 isolation; permissions are not loosened to force Layer-2 reachability.

This is a control-only repair. No DEV deployment, Stage-II dispatch, marker creation, daemon-reload, service activation, synthetic proof traffic, PROD/IAM change, or real-memory capability is authorized by the repair itself. A protected merge and read-only readiness check still stop for fresh owner authorization of `B1B2B_II / ACTIVATE_AND_ISOLATION_TEST — RETRY #2`.

## Retry #2 execution-gate completion (control-only)

The unexercised prior retry authorization does not carry forward. Future trusted `execute()` now performs the complete V2 API re-observation immediately after `daemon-reload`, before broker hardening inspection, tmpfiles, socket start, or proof traffic. It uses the authorized capture timestamp for comparison, so `ExecStart` bookkeeping (`pid=0`, `start_time=[n/a]`) remains irrelevant while genuine PID, kernel start ticks, process start, restarts, service/configuration, app/unit/custody, host, or health drift stops before further Stage-II mutation. The already-consumed authorization is preserved and failure containment stops broker/socket as applicable.

The pre-HEALTH sequence is now `capture service activation history → start socket → inspect owner.sock and unchanged activation history → negative lilith → inspect inert service/socket/owner.sock, unchanged activation history, absence of broker-UID processes, and zero database delta → negative nobody → repeat the independent inert/zero-delta inspection → relay HEALTH`. The activation-history comparison includes systemd InvocationID, actual start timestamp, restart count, and active-enter monotonic timestamp; it catches a transient start that has already exited before the immediate check. A negative-client error still triggers its immediately following inert-state inspection before propagation. Any activation or state write by a negative peer fails and prevents HEALTH. The socket remains disabled at boot. Machine-checked ordering tests fail if either independent gate is removed or HEALTH is moved earlier. Layer-1 filesystem/socket denial is preserved; no permission is weakened to force a Layer-2 negative.

The historical schema-2 used marker from attempt #1 remains byte-for-byte unchanged. Future retry #2 uses a closed `Stage2UsedAuthorizationV2` record (`schemaVersion=2`) at the distinct retry used path. It links the exact source marker SHA-256/schema, authorization ID, actor, stage/purpose, project/instance, release, API/retry baseline digests, issue/expiry, and a trusted UTC microsecond `consumedAt`. The timestamp is taken at the terminal claim, never from marker input, file mtime, or workflow logs, and must lie within the marker window and not in the future when validated. A no-overwrite hard-link creates the used path before removing the active marker; directory fsync makes that terminal claim durable. Only afterward is a fully fsynced pending record atomically published at the used path and the directory fsynced again. If publication or validation fails, the used path remains terminal, no Stage-II runtime mutation begins, and failure-stop can preserve the evidence. Duplicate consumption and historical authorization reuse fail.

This repair does not alter the synthetic proof verifier, protocol, retry delta model, installed broker release, DEV runtime, PROD, IAM, or canonical memory. The protected PR may be merged as control-only only after all gates pass; a separate fresh owner decision is still required to exercise retry #2.

## Stage III-A A2 preparation control (source only)

The existing standalone trusted control now has **separate** `stage3-*` operations. Its Stage-II operations and consumed authorization records are unchanged. The inactive-install command accepts only the already validated merge-commit release `4a04f2d09a2b32aecedfe777090fd2e1a27ec909`: archive SHA-256 `329e286b09610c6d37dab4b343f26743afa36889870cfb6593ef44f62300b66e`, 5,200,877 bytes, and embedded manifest SHA-256 `784c60d39edbd67b86d670bf568c0d28a1a6eef202c5d19254ec0f6dc74da313`. It reads the fixed private DEV staging directory, validates the exact archive and attestation, checks all 23 archive members against the manifest, and requires the installed trusted snapshot tool to report the accepted Stage-II lifecycle and complete digest `dff5ccddad5884b57f5cf895a9c86c741077fd21857d6e67de57803e6fde68e5` before any installation write.

The installer creates only a new root-owned release sibling and its offline pinned-wheel virtual environment. It does not modify `current`, `dev.json`, units, socket, API, databases, authorization state, or running processes. It checks every installed payload hash before renaming the completed staging directory into the immutable release path, then requires the same accepted snapshot digest again. A failure preserves the incomplete directory for review and fails closed; it never falls through to activation. Existing Stage-I `install-dev` remains pinned to its historical release and is not reused for A2.

The inactive-install owner dispatch is authorization-free: it never writes a Stage-III marker. The former standalone authorization-only dispatch and CLI entrypoint are removed. A later, separately reviewed **final execution** controller must call the internal authorization issuer just in time, after confirming the candidate remains installed but inactive and the accepted Stage-II snapshot is unchanged, and **before** activating the candidate or creating a short-lived challenge, proof, or arm. No final-execution workflow is registered by this repair.

The root-owned mode-0600 closed `Stage3A2AuthorizationV2` requires no prior active or used Stage-III marker or arm. In addition to exact host, source, release/archive/manifest, baseline, stage, and fault-point bindings, it pins `deploymentEnvironment=DEV`, the synthetic state profile and authority mode, logical owner and access identity, public credential record ID, credential ID and immutable-public-field fingerprint, and fixture ID/fingerprint. Issuance verifies the installed candidate's public synthetic credential against those pins. Verification reconstructs the exact V2 record and rejects missing, extra, altered, V1, noncanonical, expired, or malformed records. The ten-minute lifetime remains unchanged; any evidence that final activation cannot finish within it requires a separate review, not an implicit extension. The authorization is one-shot: the future preparation function hard-links the marker into a terminal used path and fsyncs the directory **before** asking the relay for one new `PREPARE_SYNTHETIC` challenge or constructing a synthetic proof. A crash or failure after that claim burns the authorization; it cannot be retried or silently replaced.

`stage3_arm_proposal` constructs canonical ASCII JSON for the exact closed `Stage3FaultArmV1` fields, binding the authorization ID, instrumented release, challenge ID, request/action digests, accepted Stage-II digest, fixed synthetic identity and fixture, and an unpredictable nonce. Its expiry is bounded by the challenge, authorization, and a 30-second maximum, with at least ten seconds remaining when proposed. It returns an in-memory proposal and does **not** write `/run/lilith-memory-stage3/a2-arm.json` or consume the proof. The `stage3-prepare-package` CLI is intentionally **not** registered in the manual workflow: a later separately reviewed activation/execution controller must capture its output without logging the still-valid proof and must provide its own exact activation, arm-custody, crash, and recovery gates. Merely issuing authorization does not activate the candidate or create a challenge.

The workflow registers only the manual owner-gated exact inactive-install choice. A push to protected main performs only the existing registration audit; no Stage-III operation runs automatically. This source repair itself grants no DEV installation, authorization issuance, live challenge/proof, arm creation, broker signal, recovery, or A2 execution. The private `/tmp` staged archive is transient and must be freshly reverified when a future owner dispatch is separately approved.
